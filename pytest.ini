[pytest]
markers =
    network: tests that require live network access (openFDA, MedlinePlus Connect APIs)
    llm: tests that make live Claude API calls (cost money, need ANTHROPIC_API_KEY)
    rerank: tests that need the local cross-encoder model (sentence-transformers + torch)
testpaths = tests
